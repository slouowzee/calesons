export default function _AuthStorePage() {
  // This file lives under app/ so Expo Router sees it as a route —
  // export a harmless page so the router doesn't warn about missing default export.
  return null;
}
